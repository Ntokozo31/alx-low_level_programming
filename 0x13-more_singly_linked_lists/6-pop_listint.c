#include "lists.h"

/**
 * pop_listint - delet the head node of a linked list
 * @head: pointer to the first element in the linked list
 *
 * Return: the data inside the elements that was deleted, or 0 if the list is empty
 */
listint_t *temp;
int num;

if (!head
